schema: ./src/graphql/schema.graphql
generates:
  ./src/resolvers-types.ts:
    config:
      useIndexSignature: true
      mappers:
        User: ./src/types.d.ts#UserModel
    plugins:
      - typescript
      - typescript-resolvers
